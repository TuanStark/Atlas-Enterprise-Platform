export enum UserStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  LOCKED = 'LOCKED',
  PENDING = 'PENDING',
}

export enum UserType {
  INTERNAL = 'INTERNAL',
  EXTERNAL = 'EXTERNAL',
  SYSTEM = 'SYSTEM',
}

export enum CredentialType {
  PASSWORD = 'PASSWORD',
  API_KEY = 'API_KEY',
}
